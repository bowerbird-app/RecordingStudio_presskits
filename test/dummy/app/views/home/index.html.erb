<% dummy_page_nav(title: "Dummy host", back_url: root_path) %>

<div class="flex w-full flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Dummy host",
    subtitle: "This app proves the press kit gem in a real host. It is not the product.",
    variant: :h1
  ) %>

  <%= render FlatPack::Card::Component.new(style: :default) do |card| %>
    <% card.body do %>
      <p class="text-sm leading-6 text-(--surface-content-color)">
        Sign in stays at admin@admin.com. Press kits live on the workspace, not on this screen.
      </p>
    <% end %>
  <% end %>
</div>
